# Frontend

Angular applications for the Salah Bahzad platform.

- **admin-portal/** — Teacher/Assistant back-office (this engagement). Created in the foundation phase. Angular v20+, standalone components, signals, OnPush, typed reactive forms; styled with the `--sb-*` design tokens from `docs/tokens.css` and the component specs in `docs/03-components.md`. Visual source of truth: `C:\Users\Amr\Downloads\Salah Bahzad Teacher Portal\Admin Portal.dc.html`.
- **student-portal/** — planned later.

Workspace tooling (single Angular CLI app vs. Nx monorepo) is decided in the foundation phase — see `docs/IMPLEMENTATION-PLAN-admin-portal.md`.
