# ---------- .NET ----------
[Bb]in/
[Oo]bj/
# Stray Windows-style output dirs accidentally created on macOS/Linux: the dir NAME literally contains a
# backslash (e.g. "bin\Debug", written by a misconfigured tool / Roslyn BuildHost). The [Bb]in/ rule does
# NOT match these, and a recursive **/*.cs|*.resx glob walking into them breaks MSBuild on a clean checkout
# (literal-glob CS2001 / MSB3552). Never commit them.
**bin\\Debug**
**obj\\Debug**
[Oo]ut/
*.user
*.suo
*.userosscache
*.sln.docstates
.vs/
artifacts/
[Dd]ebug/
[Rr]elease/
*.nupkg
*.snupkg
project.lock.json
project.fragment.lock.json
# Local secrets / config (never commit — NFR-SEC-002)
appsettings.*.local.json
secrets.json
*.env
.env
.env.*
# Firebase / Google Cloud service account keys (never commit)
serviceAccountKey*.json
**/serviceAccountKey*.json
*serviceaccount*.json
*-firebase-adminsdk-*.json

# ---------- App signing / store secrets (CI-only — never commit; NFR-SEC-002) ----------
**/key.properties
**/*.jks
**/*.keystore
**/*.p12
**/*.pfx
**/*.cer
**/*.mobileprovision
**/*.p8
# Per-environment PROD Flutter Firebase configs (dev configs may stay tracked — they are public client config)
app/android/app/src/prod/google-services.json
app/ios/config/prod/GoogleService-Info.plist
app/macos/config/prod/GoogleService-Info.plist

# ---------- Node / Angular ----------
node_modules/
dist/
.angular/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
coverage/
**/.nx/cache/
**/.nx/workspace-data/

# ---------- Editors / OS ----------
.idea/
*.swp
.DS_Store
Thumbs.db

# ---------- Claude / local ----------
.claude/settings.local.json
